<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.agent.mapper.LogMapper">
    <resultMap id="BaseResultMap" type="org.example.model.entity.Log">
        <id property="id" column="id" jdbcType="VARCHAR"/>
        <result property="userId" column="userId" jdbcType="BIGINT"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,userId,content,createTime
    </sql>

    <select id="selectByDates" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM log
        WHERE DATE(createTime) IN
        <foreach collection="dateList" item="date" open="(" separator="," close=")">
            #{date}
        </foreach>
    </select>
</mapper>
